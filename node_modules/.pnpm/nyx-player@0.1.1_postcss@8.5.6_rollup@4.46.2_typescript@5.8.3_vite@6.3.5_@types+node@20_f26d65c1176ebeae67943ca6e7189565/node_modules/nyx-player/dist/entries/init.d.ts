import { Preset } from '../presets';
export declare function initPlayer(el: string, showBtn: string, urls: {
    url: string;
    name: string;
}[], playBtn?: string, darkModeTarget?: string, preset?: string, styles?: Preset): void;
